# Jekyll build output & caches
_site/
.jekyll-cache/
.jekyll-metadata

# Local gems (installed into the project by the Nix dev shell)
vendor/
.bundle/

# direnv
.direnv/

# OS cruft
.DS_Store
Thumbs.db

# Full-resolution photo archive — kept locally, backed up outside git.
# The site only serves derivatives in assets/img/photos/. See README.md
# ("Images: quality, sizes, storage, limits") and originals/README.txt.
originals/
!originals/README.txt
